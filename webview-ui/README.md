# NeverMIN webview UI

Vite + React + TypeScript + Tailwind app for the Code Graph panel.

```bash
npm run build          # from webview-ui/ → ../dist/webview
npm run build:webview  # from repo root
```

Reload the VS Code/Cursor window after rebuilding so the extension picks up new assets.
